# Port-debt registry

**Full port, not MVP.** Every synthetic / MVP / "good enough for now" shortcut that could
**silently cap 1:1 parity** is registered here with a retire condition, so a fake never
hides behind a green test suite. This is the OpenSummoners analog of openrecet's
`port-debt.md`.

## The convention
- When you take a shortcut that diverges from retail behavior (a synthetic value, a
  skipped arm, a zero-init where retail does something, a constant where retail computes),
  add an inline **`PORT-DEBT(tag): …`** comment at the site AND a row in the table below.
- The `tag` is a short kebab-case slug, unique, cited in the row and the code.
- A row needs: **what's faked**, **why** (why it was safe to defer), and the **retire
  condition** (the concrete event that forces it to be made faithful — usually "when
  subsystem X is ported" or "before scene Y can be bit-exact").
- Retire by porting the real behavior, then delete the comment + the row.

## NOT port debt (don't register these)
The host-build vs Win32-build split is **by design**, not debt: `*_win32.c` real impls
with recording stubs in the host test harness, and `calloc`-for-determinism where retail's
`operator_new` leaves bytes uninitialised that are then always explicitly written (same
observable result). Only register a zero-init/stub when it produces a behavior that
**differs from retail's observable output**.

## Open debt

| tag | what's faked | why safe for now | retire when |
|-----|--------------|------------------|-------------|
| `map-zero-init` | `game_map.c`: the `0x4120` map object is zero-initialised; retail's `operator_new` is raw + filled by opaque sub-inits (`0x5612b0`/`0x5611d0`/`0x4e59a0`). The `+0x4020` ramp ceiling is one of those outputs, so the `+0x4014` ramp is inert under zero-init. | The verified room-key path (`+0x4024 = 0x334be` → room 210110) doesn't depend on the ramp. | the in-game sim reads `+0x4014`/`+0x4020` for anything visible. |
| `map-blend-va` | `map_decode`: the two `0x1b58d` blend pointers `&DAT_005cc410`/`&DAT_005cc430` are stored verbatim as their retail VAs (`MD_BLEND_*`) in region B `+0x8`. | The render port hasn't consumed them yet; storing the raw VA preserves the exact bytes for later translation. | the render port dereferences region B `+0x8` (translate VA → ported blend object). |
| `map-emit-tile-params78` | `map_grid_emit_tile` drops decompiled params 7/8 of `0x58c910` (see `map_grid.c`). | Unused by the 9 town tile ids exercised by DATA 1022. | a map/tile id that passes non-default params 7/8 is decoded. |
| `render-palette-tint` | The in-game **colour-grade LUT** (`DAT_008a9410`, `FUN_00562ea0`'s cosine tone curve).  **8bpp half DONE (ckpt 67):** `color_grade.{c,h}` + `main.c` grade every in-game 8bpp sheet's palette — the backdrop TILES are `differ_px==0`.  **24bpp parallax grade DONE (ckpt 68):** `game_arm_parallax_grade` (`main.c`) replicates `0x417c40`'s flag-3 stamp (`f_08=1`, scales 1000, `f_18`=LUT) before the parallax getter, so `ar_sheet_decode_pixels` LUT-grades the sky/mountain banks (`0x55`/`0x58`/`0x59`) at decode — blue now `239`, matching retail's main band.  **Grade USER-CONFIRMED correct (ckpt 68).** The flat per-channel LUT base is matched (blue `239` == retail). **OPEN (deferred):** a "dark top gradient" the user sees in the establishing-scene frame (but not in settled gameplay) — working hypothesis: a **per-scene CINEMATIC effect tied to the establishing shot**, to be confirmed by probing ground truth alongside the intro PAN RE (`ingame-camera-snap`). NB the per-sprite *tint* switch (`DAT_008a93fc`/`0x4182d0`) is identity (0) for the town. | Tile + 24bpp sky base colour are matched/confirmed; the cinematic gradient is additive and scene-scoped. | the establishing-scene gradient is probed/RE'd (with the pan) and layered on, and the cameras align for a px-exact sky diff. |
| `render-regionC-blend` | `map_render`: the region-C blend/overlay arms (`0x1b58d`/`0x1b5ab`, `490f30.c:230-282`) are not walked. | The town backdrop's base tiles render without them; isolate first. | the backdrop diff shows the blend/overlay objects. |
| `present-actor-modes` | `map_present` (`0x48eac0`): **mode 0 (opaque actor keyed, `FUN_005b9b70`) is ported AND now WIRED** (ckpt 77 port + ckpt 79/80 wiring — `town_render_step_ex`'s actor seam + `main.c game_actor_walk` emit the 5 props via `actor_render_static` and the wagon `0x1872d` via `actor_render_protagonist` → `PRESENT_KEYED`, cel-dims cull via `present_dims_fn`).  **Mode 1 (alpha, `FUN_005bd550`) is now ported for the simple cel case (ckpt 88)** — `map_present` case 1 projects (cel-dims cull) → PRESENT_ALPHA, and `game_present_blit` orchestrates via `zdd_blit_orchestrate` + `g_ramp_a` (the fountain spray `0x18708`; node param8 carries the ramp index).  **Mode 2 (scaled, `DAT_008a9274` palette)** stays VISITED-but-deferred (reported via `out_deferred`, never dropped). | Mode 2 reads paint_ctx/palette internals beyond the cull (sprite `+0xc..+0x28`, `DAT_008a9274`); no town actor drawn so far is mode 2.  Mode 1 for the fountain is the simple cel-alpha orchestrate (no per-pixel palette). | a SCALED actor exercises mode 2; a mode-1 actor needing the full paint_ctx blend (vs the simple ramp orchestrate) appears. |
| `actor-occlusion` | `actor_render_static`: the tile-occlusion mark `0x4927c0` (`0x491ae0:376`, writing the grid's occlusion buffer `grid+0x190030` so backdrop tiles BEHIND an actor are culled) is skipped. | It culls backdrop tiles behind the actor, NOT the actor's own blit; the tiles already matched retail at flip 1500 without it (actors weren't drawn). | the actor walk is wired and a backdrop tile behind an actor diverges (retail culls it, the port draws it). |
| `actor-sprite-table` | `actor_spawn.c`: the visible CHARACTER codes' `+0x48` sprite rows come from a hardcoded captured map (`TOWN_SPRITE_DEFS`: `0x1129e`→bank `0x16c` f1, `0x1129f`→f2, `0x112e5`→f36 layer 10) — the live town-hold capture (quirk #80) standing in for the lazy def-table fill (`FUN_0040afe0`/`FUN_0041e600`, keyed by actor type+state).  Room-specific (DATA 1022); the 29 invisible codes correctly get bank 0. **NB the FILL PRIMITIVE is now RE'd (quirk #81): `FUN_00426db0(dir, bank, frame_base, b, x_off, mirror_x, y_off)` writes one `actor_sprite_row` — what's still un-RE'd is the type-keyed DEF TABLE the state-set passes to it.**  (The wagon `0x1872d`'s install IS fully RE'd: `0x426db0(0, 0x175, 0, …)`, used directly by `actor_spawn_protagonist`.) | The captured values are retail ground truth; the spawn reproduces the end state exactly (invisible volumes = bank 0); the town is the only in-game scene. | the type-keyed sprite/anim def table the state-set reads is RE'd + extracted (any room's actors get sprites data-drivenly) — then `TOWN_SPRITE_DEFS` is deleted. |
| `actor-protagonist-clip` | `actor_spawn.c`/`actor_render.c`/`main.c`: the town intro CARAVAN `0x1872d`'s horses now **TROT** — `actor_pool_update`→`actor_anim_advance` (the `0x46cd70`/`0x54f980` per-tick stepper, half 1 / quirk #82) advances `WAGON_CLIP` once per sim-tick on the camera cadence, so the body cel cycles sprite 2..5 (LIVE-verified: res `0x3ec` body steps 2→3→4→5→2 every 36 Flips at the settled cam; host-tested).  REMAINING synthetic: (a) the wagon's idle-wander **RNG draws** are now PORTED consume-to-advance (ckpt 99, `ambient.c` `wagon_timer_step` = the `0x54f980:932-966` idle-wander timer, fires tick 134 in the settled town) so the shared stream stays aligned — but the resulting **MOTION** (the `0x43f880` movement FSM the drawn wander offset feeds) is still deferred (the wagon holds position; PORT-DEBT(butterfly-wander) is blocked on the same FSM); (b) the spawn world pos is the settled census const (54400,32000) — **USER-confirmed correct as the PARKED position** ("the wagon doesn't move … which is how it's supposed to be"); whether the caravan visibly ROLLS IN earlier in the intro (the cutscene's anchor-relative `0x431d10(…,0x65,0x3200,…)` arrival) is a separate, un-RE'd `0x4d7d80` question — not observed at the settled hold. | The idle-animating composite is bit-verified + USER-confirmed at the settled camera (horses idle subtly — ear flicks; wagon parked); `WAGON_CLIP` is 4 RE'd shorts (timing metadata), not the binary asset.  The remaining behaviour is the RNG layer (deferred by direction). | the `0x4d7d80` cutscene drives the roll-in / anchor spawn; the RNG behaviour lands with the scene-wide RNG-consumer census.  (If many more clips are needed, read them from the user's `sotes.exe` `.rdata` at runtime rather than reconstructing each.) |
| `effect-sprite-table` | `actor_spawn.c`: the EFFECT townsfolk's `{code → bank, dst, layer}` comes from a hardcoded captured map (`TOWN_EFFECT_DEFS`: 10 `0xc3xx` codes → banks `0x93`–`0xf9` + `0xe2a5`→`0x14c`, dst ≈(-30,-2x), layer 13) — the live town-hold census (the `0x493ba0` field spec, flips 1450/1500/1600) standing in for `FUN_0041f200`'s per-type install switch (a 27 KB function, captured like the CHARACTER band's `actor-sprite-table` rather than RE'd case-by-case).  The MAP supplies the position (world = (map (x,y) − dst) × 100, derived + verified cel-for-cel against the census `rs_x`); only the appearance anchor is captured.  **ckpt 85: extended with `facing` (1/3) + `flip` (frames-per-dir) columns** — facing is the deterministic map field `puVar1[4]` (dispatcher `0x58d460:96`), flip = `*(s16)(DAT_008a8440[bank])`, both captured live; the townsfolk now mirror (USER-1:1). | The captured values are retail ground truth; the spawn reproduces the end state; the town is the only in-game scene so far.  Each townsperson emits ONE mode-0 keyed cel (verified vs the hold blit trace), so the render reuses `actor_render_static`. | `FUN_0041f200`'s town cases (bank/dst/layer install) are RE'd + extracted, facing comes from the map `puVar1[4]` (port `0x587e00`), flip from the loaded sprite-group descriptor — then `TOWN_EFFECT_DEFS` is deleted. |
| `effect-anim-phase` | `actor_spawn.c`: the EFFECT townsfolk are spawned FROZEN on the idle clip's frame 0 (`clip NULL`, `frame_base 0`) — the static positions render 1:1 but the idle-breathing POSE is stuck.  The clip itself IS decoded (`0x6290e0` = base 0, 20 frames, dur 14, looping, delta cycles {0,1,2,3}; `0xe2a5`/`0xc35a` share it; `0xe29a` = a 3-frame walk `0x65ddf0`) and the per-sim-tick stepper (`actor_pool_update`/`actor_anim_advance`) is already ported — what's missing is the per-actor START PHASE — **CONFIRMED RNG (ckpt 85):** `FUN_00426ec0` sets `rs+0x72 = (rand() * clip.frame_count@+0x42) >> 15` (a uniform start frame) + a 2nd rand for the timer; the live census confirms every townsperson runs `0x6290e0` at a scattered start frame — so it needs the **game_enter RNG anchor**, not a deterministic source. | Positions + sprite identity are 1:1; the frozen pose is a ≤1-cel idle-breath difference (render_diff: the on-screen townsfolk match on res + dst, differ only on FRAME).  Frame 0 is the literal spawn end-state (pre-update). | the per-actor anim phase source is RE'd (likely the scene-wide RNG-consumer census, Phase 2) + the EFFECT update fn (`0x54e5c0`/…) cadence is pinned, then attach the decoded clip + seed the phase → the townsfolk breathe 1:1. |
| `effect-color-variant` | `actor_spawn.c`/`actor_render.c`: EFFECT townsfolk render with the DEFAULT colour variant (`frame_base 0`) — the per-instance colour/appearance VARIANT the retail town assigns is not applied, so some townsfolk show the wrong palette.  **OBSERVED (trace-studio `intro-1`, ~tick 276 — the townsgirl by the INN/barrel, a generic map EFFECT actor, NOT the wagon-anchored cutscene cast):** the port renders her brunette in a blue dress, retail blonde in a pink/red dress — SAME base sprite + arms-crossed idle pose, a different palette (NOT a uniform colour-grade shift: hair brown→blonde, dress blue→pink).  Mechanism un-RE'd: most likely the per-instance render-side **`frame_base`** (0/4/8/12) selector — the SAME class as PORT-DEBT(`butterfly-wander`)'s multicolor item — or a per-instance colour-remap (`DAT_008a9358`); confirm at fix-time via the emit `0x492670` `frame_base`/remap field. | Position + sprite identity (bank) + facing are 1:1 (`effect-sprite-table`); the divergence is the colour variant only, on background townsfolk. | the per-instance variant source is RE'd (the `0x492670` emit `frame_base` ↔ instance map, or the `0x8a9358` colour-remap) and applied — also clears the butterfly multicolor item (same selector). |
| `butterfly-wander` (was `effect-wanderers`) | `actor_spawn.c`/`butterfly.c`: the 4 `0xe29a` BUTTERFLIES render (ckpt 96), their per-tick LCG draws are aligned (ckpt 98), and as of **chip 1 (ckpt 110) the open-air PATROL MOTION is PORTED + field-exact**: the `0x47b990` `0xe29a` heading FSM (cooldown + the within-1-tile/10%-roll flip), bounds set at register (`0x14264/0x14268` = spawn_wx ± 11200/−8000), and the apply-pass horizontal integrator (the open-air reduction of `0x485fc0`→`0x442a70`: hvel ramps ±10/tick→±100, step-before-ramp).  Validated vs `runs/butterfly-fsm` on the sim-tick axis: **heading 0 mismatches** (every flip tick exact, all 4 butterflies, ticks 0..269), worldX field-exact between reversals.  STILL synthetic: (a) the **VERTICAL flutter** + the flap/reversal COUPLING → PORT-DEBT(butterfly-flutter) (the only worldX residual: a ≤170-unit ≈ ≤2px bounded transient at turn-arounds, non-accumulating); (b) per-instance **frame_base 0 / facing-direction variant** (USER-noted ckpt 110) → all render one variant.  RESOLVED (capture): the drawn `cel_fr` (`rs+0x72`) base is **always 0** for every (heading,facing) — it is the heading-INDEPENDENT looping flap, NOT the variant selector; so the per-instance colour/direction is the **actor sprite frame_base** (0/4/8/12, a render-side selector), distinct from the `body+0x2c` mirror (which IS ported, ≤1/286 exact). | The butterflies now **patrol left↔right 1:1** (heading/facing field-exact; the dominant visible motion), the per-tick stream stays bit-exact (host `butterfly_motion` + `butterfly_pertick`; the exact flip ticks PROVE the LCG is byte-aligned through tick 269). | the full integrator (chip 2/3) ports the vertical flutter + flap coupling (clears butterfly-flutter) → fully 1:1; + the per-instance **sprite frame_base** 0/4/8/12 (RE the emit `0x492670` frame_base ↔ instance/heading map) for the white variant + directions. |
| `butterfly-flutter` | `butterfly.c`: chip 1 ports only the open-air HORIZONTAL patrol; the VERTICAL flutter is deferred — the butterfly's `body+0x18` velocity sawtooth (ramp +2000/tick→+16000, snap to −30000, ramp back; driven by the `cmd_2` flap sub-FSM = `0x442a70` case-3 `:832-877`) + the worldY bob it integrates (`0x54e5c0(body, vel/100)` → `body+8`).  Its FLAP impulse also couples a ±100 worldX lurch when it coincides with a heading reversal — the sole worldX residual vs `runs/butterfly-fsm` (≤170 units ≈ ≤2px, bounded/non-accumulating because the heading flips are field-exact).  worldY currently holds the spawn value (butterflies glide flat, no bob). | The horizontal patrol (the visible left↔right drift) is field-exact; the vertical flutter is a secondary bob whose exact sawtooth needs the 12 KB integrator's case-3 flap sub-FSM (shared with Arche). | the full `0x442a70` integrator is ported (chip 2/3) — the flutter sawtooth + flap-coupling land with the grounded/Arche physics. |
| `butterfly-bounds-writer` | `butterfly.c`: the patrol bounds `0x14264/0x14268` are set at register-time as `spawn_wx + 11200 / − 8000` (the ckpt-109 capture's dead constants, field-exact for the town).  Their real spawn-time DERIVATION (no static writer; set via a computed pointer off `0x428780`'s `+0xc890/+0xc894` wander params) is un-RE'd. | The values are bit-exact for the town's 4 butterflies (capture-pinned, constant across all 286 ticks); the formula `center spawn_wx+1600 ± 9600=3·0xc80` is understood, only its writer isn't. | a butterfly (or other `0x47b990` wanderer) spawns where the +11200/−8000 const is wrong — `mem_watch this+0x14264` at spawn to RE the writer (chip 2/3). |
| `collision-slopes` | `collision.c`: the vertical tile-mover (`0x54e990`) is ported for FLAT collision (region-B class **10**=solid wall / **1**=slope surface), but the SLOPE-height resolution is a caller callback — region-B `+0x8` holds an engine `.rdata` VA (the `0x5cc410`/`0x5cc430` height profiles, written verbatim by `map_decode`) that the host can't deref.  The town street is flat (every collision cell's slope ref is 0) so the callback is unused; sloped areas (stairs/hills) need the real 32-byte profiles resolved. | The town has NO sloped collision cells (`map_decode` emits a nonzero slope ref only in the `0x1b58d` arm, not hit by the flat street) — flat collision is exact + host-tested (`test_collision.c`); the callback keeps the mover faithful + ready (the `d==0` slope-contact predicate is ported verbatim). | the `0x5cc410`/`0x5cc430` height-byte profiles are read from the user's `sotes.exe` `.rdata` + a live resolver wired (chip 3, when Arche walks a slope). |
| `char-run-trigger` (was `char-run-jump` → `char-run`) | `character.c`: the RUN (dash) PHYSICS is now PORTED + bit-exact (chip 3b, ckpt 118 — the `0x442a70` case-0x75 run branch: cap `in_ECX[0x5664]`=48000 + the two-phase accel `in_ECX[0x565d]`=3200 to 24000 then 1600 to 48000, validated tick-for-tick vs `runs/runjump-gt/capdash2`, `test_character_run_ramp`).  What remains synthetic is the run TRIGGER: `character_step`'s `run` arg is the *resolved* cmd[0]==5/6, but the AI's double-tap DETECTION (`0x478ba0`→`0x479e70`/`0x479960`: two direction ring events within the window `*(*0x8a6e80+0xf8)`, then self-sustain while held) is NOT modelled in the port (the port replays the held AXIS, not the discrete ring). | The run cap/accel are ground-truth (live-captured consts) + bit-exact validated; the trigger is an INPUT-detection layer the port can't exercise until the live wire (the held-axis replay has no ring double-tap).  The `run` arg cleanly separates the validated physics from the deferred detection (mirrors `jump_held`). | the live wire (chip 4) gives `0x478ba0` real ring access → port `0x479e70` (the double-tap scan) so `run` derives from the input, not the caller → a USER double-tap dashes on screen. |
| `char-jump-variable-height` | `character.c`: the jump's airborne integrator is bit-exact to BOTH the captured **SHORT HOP** (ring-tap → cmd[2]==0 the rise → FREE grav `in_ECX[0x5669]`=8000) AND the captured **HELD high-jump RISE** (held-C → cmd[2]=8 → FLOATY grav `in_ECX[0x5668]`=2000) — the held-C capture (ckpt 117, `runs/runjump-gt/capheld`) validated the 2000 branch field-exact for its 16 rise ticks (`test_character_jump_held_rise`).  The ~7-flip WINDUP is now RE'd + ported bit-exact (ckpt 119): the launch lags the trigger by exactly 4 stationary ticks (`0x442a70:834-841` case-3 sub-state-0 counter>4; `test_character_jump_windup`, ground-truthed off the `bstate` field of `capjump-ring2`).  STILL synthetic: retail's held apex CLAMPS on a **town CEILING** at wy≈41600 (the `wy += vvel/100` relation breaks = a collision clamp) — the flat-ground port has no ceiling, so a held jump keeps rising at 2000 (= the `char-collision-mover` debt, NOT a jump-physics gap). | Both rise gravs are ground-truth (captured off the entity) + bit-exact validated, and the windup is now bit-exact; the only un-modeled piece is the ceiling (a collision concern, deferred with `char-collision-mover`). | the ceiling lands with `char-collision-mover` (the real vertical mover `0x54e5c0` clamps the apex on terrain) — at which point this row folds entirely into `char-collision-mover`. |
| `char-jump-fall-grav-source` | `character.c`: the jump FALL grav (`CHAR_JUMP_FALL_GRAV`=4000, while `vvel>=0` + the launch tick) AND the terminal fall velocity (`CHAR_JUMP_FALL_TERMINAL`=64000, the cap observed bit-exact in the held jump's 8-tick plateau) are pinned bit-exact by the captures, but their in_ECX SOURCE is unknown — NEITHER is in the per-entity move-tuning band `0x565a..0x566f` (all captured ckpt 117).  Likely GLOBAL gravity constants read directly in `0x442a70` (note 4000 = 8000/2 = the free rise grav halved; 64000 = 16·4000). | The values are ground-truth (the arcs), so the port is bit-exact; only the provenance/annotation is incomplete (constants vs globals vs derivations). | the fall grav + terminal's source is located (a `mem_watch`/wider const scan or the `0x442a70` global read) → they derive from the real source, confirming they generalise (other characters / the in-air fall cap). |
| `char-input-autorepeat` | `character.c`: the press→latch warmup is a constant `CHAR_INPUT_REPEAT_DELAY`=3 (motion starts 2 idle ticks after the press), reproducing the seed-pinned capture; retail's real source is the wall-clock GetTickCount auto-repeat threshold in `0x478ba0` (`0x479e70`/`0x479960`/`0x479ca0`).  Once latched the walk self-sustains deterministically. | The warmup is a 1-shot transient at the start of a press (the deterministic accel/cap/brake is the validated meat); under lockstep the capture shows a consistent 2-tick delay; aligning at motion-start the steady law is field-exact (the butterfly-chip-1 "exact between reversals" precedent). | the `0x478ba0` auto-repeat (`0x479e70` et al.) is ported with its real ms threshold — then the initial-repeat delay derives from the wall clock, not the constant. |
| `char-walk-tuning` | `character.c`: the walk accel/cap/brake are the `#define`d constants `CHAR_WALK_ACCEL`=1600 / `CHAR_WALK_CAP`=24000 / `CHAR_WALK_BRAKE`=800.  **Their per-entity move-tuning SOURCE is now CONFIRMED (captured live off Arche's entity, ckpt 117, `runs/runjump-gt/capconsts`):** `in_ECX[0x565b]`=24000 (cap), `in_ECX[0x565c]`=1600 (accel), `in_ECX[0x565e]`=−800 (brake — signed) — the ckpt-115 hypothesis PROVEN.  The port still hardcodes them rather than reading the entity at runtime. | The constants are bit-exact for Arche's town walk AND confirmed to be the entity's tuning fields (not curve-fit); a different character would carry its own tuning, but the port reads the same values. | the port READS `in_ECX[0x565b/c/e]` from the live entity (when Arche is wired live, chip 4) → the tuning derives from the entity per-character, not a `#define`. |
| `char-collision-mover` | `character.c`: the worldX commit is a FLAT reduction `world_x += vel/100` (the town street is flat); retail commits through the collision-aware horizontal mover `0x54db10` (sibling of the chip-2 vertical `collision_move_vertical` `0x54e990`).  **The JUMP's vertical worldY commit + landing is ALSO a flat reduction:** retail steps `worldY += vvel/100` through the vertical collision mover `0x54e5c0` (which returns blocked on ground contact → vvel=0); the port models a flat clamp at `ground_y` (the spawn worldY).  Also the reverse-decel rate (commanding the opposite direction while moving) uses the brake rate here; the decompile uses −accel, but the capture has an idle gap so a direct reverse is untested. | The captured walk + jump are open-air flat (the town street is flat, the jump lands on it); the chip-2 collision read-side is ported + host-tested and wires in here when a grounded actor needs it; the reverse path is structurally faithful but un-validated. | `0x54db10` (horizontal) + `0x54e5c0` (vertical jump) are ported + wired (their first live caller with Arche) → worldY/worldX clamp to real terrain (slopes/ledges) + a reverse capture validates the −accel decel → Arche stops at terrain + turns + lands on slopes 1:1. |
| `char-control-trigger` | `main.c`: Arche becomes controllable at a MEASURED frame (`CHAR_CONTROL_ARM_FRAMES`=200 flips after game_enter) — the chip-3c LIVE WIRE (ckpt 120) arms `character_step` there (her first live caller in `game_actor_update`) and mirrors its world pos/facing into her render-state.  Retail transfers control at the dialogue's "PLAYER!" prompt (~game_enter+3067, ckpt 112) via the town script setting the per-actor control flag `entity+0x200=1` (`0x41e070`/`0x4c6830`); the port's dialogue (line 1 only) does not yet drive that hand-off, so the arm frame is a measured stand-in (like the banner/dialogue triggers).  Also `run` is hardwired 0 (no live double-tap source — see `char-run-trigger`). | The arm frame is in the settled town (post-pan, pre-dialogue) — a clean window to demonstrate + capture the controllable mover; the control LOGIC (the mover, host-validated bit-exact) is the validated part, only the arm TIMING is synthetic. | dialogue chip 4 drives the real ~15-line town script → the `entity+0x200=1` hand-off arms `character_step` (the same unported driver `0x4d7d80`→`0x439690` as the banner/dialogue/pan triggers). |
| `cutscene-party-chars` | `actor_spawn.c`/`party.c`/`asset_register.c`: the town-intro arrival cast — **Dr. Barnard** (`0xc3f0`→`0xeb`), **Arche's Father** (`0xc3dc`→`0xe3`), **Arche's Mother** (`0xc440`→OVERRIDE `0xb5`), and now **Arche** herself (`0xc35a`→body bank `0x8b`, ckpt 94) — is spawned into the EFFECT pool and rendered via `actor_render_static` (one keyed cel each).  STILL synthetic: (a) Arche is rendered as a **static-cast member**, NOT yet via the party band `0x4997b0` (the persistent leader path) — and only her **row-0 body bank `0x8b`** is drawn; her multi-part banks `0x8c`-`0x8e` (other poses) are registered but unused.  So the chip-3c LIVE WIRE (ckpt 120) drives her world pos + facing live (`character_step`) but the RENDER is still the single static bank on the IDLE clip: she SLIDES without a walk-cycle, and does NOT mirror when facing LEFT (`facing==3` selects `frame_base + flip_table[0x8b]`, and `0x8b` has no mirror-frame registered → she stays right-facing; no position glitch); (b) positions are the wagon's SETTLED anchor (`CUTSCENE_WAGON_ANCHOR_X`=41600) + the RE'd / census offsets — the **walk-in DIALOGUE movement** (the family animating to these spots, the actor FSM `0x402730`/`0x402330`) + the **live-actor handle registry** (`0x556eb0`, speaker resolution) + the portrait/textbox (`0x5a00c0`) are Phase 3. | **USER-confirmed (ckpt 94): "everyone is rendering correctly now."**  The full arrival cast renders 1:1 vs the retail settled town; bit-level confirm (port blit trace) res `0x570` at screen (258,304) = Arche's computed pos.  Arche's body banks are EXE-embedded (res `0x570`-`0x573` in sotes.exe, `ar_register_party_exe_sprites`, loaded from the user's file — never embedded). | the party band `0x4997b0` + multi-part body render (Phase 2) makes Arche the controllable leader; the cutscene FSM + dialogue (Phase 3) drive the walk-in + the portrait/textbox. |
| `dramatist-archetype-defaults` | `party.c`: `party_archetype_default_bank` models only the RE'd subset of `0x41f200`'s per-archetype `if (sVar17==0)` default-bank cases (the town-arrival archetypes + neighbours: `0xc3dc`/`0xc3dd`/`0xc3e6`/`0xc3eb`/`0xc3f0`-`0xc3f3`/`0xc440`/`0xc441`).  `0x41f200`'s switch has hundreds of cases. | These are the cases read off the decompile that the arrival cast (+ the town villagers) exercise; an unmodelled archetype returns 0 (the caller skips/defers, never mis-renders). | a scene spawns a dramatist whose archetype is outside the subset — RE that `0x41f200` case + add its row. |
| `decode-prologue` | `map_decode`: `0x587e00`'s prologue (front-header flags + HUD/border bank selection over `DAT_008a76xx` + the `0x1bd82` autotile pre-pass) and trailing layer pass (`0x58c8c0`/`0x58c8d0`/`0x58cb30`) are deferred. | Dead code for the opening town (every town tile id `< 0x1bd82`; no HUD/border in DATA 1022). | a map that exercises HUD/border families, autotile, or the layer pass is decoded. |
| `ingame-camera-pan` (was `ingame-camera-snap`) | The intro-pan EASER (`FUN_0043d1d0`) + the scripted target-setters (`0x439690:599-642` SNAP/`643-664` PAN command arms) **are ported AND WIRED LIVE (ckpt 70)** — `main.c game_render` steps a live `camera_view` (`camera_follow_step` + the `CALL_TRACE_BEGIN(0x43d1d0)` flow-trace mirror), `enter_game` spawn-snaps it via `camera_apply_snap` (cur=tgt=128000/12800), and a hold timer fires `camera_apply_pan(12800,12800,300)` at hold-end → the backdrop pans left from the hold to the settled town-left-edge view (visually confirmed on the feed: hold→mid-pan→settled). `camera_apply_snap`/`_pan` are host-tested bit-exact (target clamp to `[0, map-vp]`, snap-jumps-cur / pan-keeps-cur). **CADENCE + TRIGGER now MEASURED (ckpt 70b) → port matches retail's trajectory.** A retail field-spec trace pinned both: the easer fires **once per 2 Flips** (`game_camera_step` gates the sim to every 2nd frame) and the pan command fires at **`game_enter + 184` Flips** (`GAME_CAMERA_HOLD_FRAMES=184`). The port now passes through the **identical `cam_x60` sequence** as retail (128000,127990,127970,127940,…,cruise −300/2flips — verified by diffing the captured `0x43d1d0` mirror).  **TICK-AXIS recalibration (ckpt 105): `GAME_CAMERA_HOLD_FRAMES`=**182** — the pan command lands on sim tick 92 (camera first moves on the t93 present, = retail; the flip-axis 184 was 1 tick late, the error absorbed by retail's present-coalescing, quirk #99); at tick-equal the whole pan now matches with only the particle-ensemble residual (~2k px).** **REMAINING:** (a) the **startup-jitter PHASE** — retail's sim accumulator is wall-clock-paced (a 4-Flip plateau at 1618-1621, a double-tick at 1616) that a clean fixed 2:1 step doesn't reproduce → a ~2-3 Flip phase offset vs retail mid-pan (≤1 step = 300u ≈ 3 px, transient; zero at hold + settled); needs the engine pace clock (map-object `+0x4068`); (b) the cutscene-script **TRIGGER source** (the `0x5a00c0` op that writes the `+0x4c` command — vs the measured hold timer); (c) the spawn-snap origin is the live const (128000/12800), not yet derived from the entry params (`+0x4028`/`+0x402c` via `FUN_004c5350`). The SETTLED end (`MAP_RENDER_CAM_TOWN_3F2_SETTLED`, x=y=12800) is a determinate static camera both sides share. | The camera lives + pans at retail's exact trajectory (identical scroll sequence); easer formula + target-setters byte-verified; cadence + trigger measured from ground truth; hold + settled ends are determinate constants both sides share. | the wall-clock sim-tick accumulator + the cutscene-script trigger source land (both downstream of the in-game sim / `0x5a00c0` port), removing the ~2-3 Flip startup-phase offset for a perfectly flip-anchored pan diff. |
| `exe-null-banks` | The EXE-NULL sprite banks `0x570-0x572` (registered with `settings=NULL`, sourced from `sotes.exe` as a datafile) are not yet registered at the engine-time site. | The slot indices haven't surfaced; the backdrop's base banks load lazily. | the in-game backdrop references a `0x570-0x572` bank (decode diverges without it). |
| `ingame-nontile-layers` | `main.c game_render`: the mid-plane TILE grid (`map_render_walk` mode-3 nodes), the **parallax sky/mountain far-plane** (`parallax_render`, ckpt 66), the **area-title BANNER** ("Town of Tonkiness", `0x494a60` mode 1 → `banner.c`, ckpt 101), and now the **DIALOGUE BUBBLE** (the `0x48c820` widget tree → `dialogue.c`, ckpt 104 — line 1 bit-exact incl. pop-in/portrait/typewriter) are drawn.  STILL deferred: the **caption / scrolling story-text overlay** (`0x5a00c0`, the prologue-narration runner with its own draw-list + the `DAT_008a7640` font bank).  ALSO: the parallax's per-room params are hardcoded for the town (`TOWN_RENDER_PARALLAX_P2/P3` = 4/1) instead of derived from `room[0x44]`/`room[0x43]` via `game_map`/`game_world`. | The backdrop tile + far-plane + banner + dialogue layers are provable + asset/scale-correct standalone (golden + user-confirmed ckpt 66/101; dialogue differ_px==0 in-window ckpt 104); the remaining non-tile layer is a separate producer; the town is the only in-game scene so the hardcoded params are exercised. | the caption (`0x5a00c0`) producer lands + the room→prologue-param plumbing is wired (this row narrows as each lands). |
| `banner-trigger` | `main.c`: the area-title banner is armed on a MEASURED timer (`BANNER_ARM_FRAMES`=**82** flips after `game_enter` = first alpha step on **sim tick 42**, TICK-AXIS calibrated ckpt 105 — the original flip-axis +78 was 2 ticks early; retail's present-coalescing plus the 2.5-tick alpha-ramp plateaus hid the error, quirk #99) rather than by the scene script.  The text "Town of Tonkiness" + the hold duration 400 are also passed in (hardcoded for the town) instead of read from the area-name table / the script's banner-arm command. | The tick-42 first step is per-present-luminance calibrated against retail (fade-in AND fade-out edges land differ_px==0 at tick-equal); the town is the only in-game scene; precedent = the camera-pan + letterbox triggers (same unported scene-script source, same measured-constant treatment). | the scene-script banner-arm command is RE'd + ported (it sets `enable`/`mode`/the text/`hold_dur` from the area-name table `0xd2` → "Town of Tonkiness") — then the arm time + text + duration derive from the room/script, not constants. |
| `banner-grade` | `main.c` (the decode hook): the in-game 8bpp palette colour-grade is applied GLOBALLY to every 8bpp sheet (`g_color_grade_on`), but retail gates it on the `0x417c40` graded getter — the banner scroll (slot 53/res `0x449`), the letterbox (slot 41), and the dialogue-widget UI sheets (slot 50/res `0x456` bubble + slot 52/res `0x44a` name tab, resolved via the plain `0x4184a0`/`0x418470` family) are NOT graded.  The port skips the grade for slots 53/50/52 by SLOT INDEX (hardcodes) to make them bit-exact, rather than modelling the getter distinction. | The skipped sheets are the visible plain-getter sheets (banner ledger #11; dialogue ledger #12 — an exact-pixel match of the ungraded tab/portrait against live retail frames proves no grade); the town is the only in-game scene. | the decode grade is gated on the `0x417c40` graded-getter path (set a per-slot "grade" flag at the graded blit sites; tiles/sky get it, plain-getter sheets don't) — then the slot hardcodes are removed. |
| `banner-font-table` | `banner.c`/`main.c`: only the font-6 path of `0x494a60`'s length→font ladder is ported (Courier New h20 w10, used for area names of length ≤ 22 — the town's 17).  Names of length ≥ 23 select retail's font idx 8 (`DAT_008a9274[8]`, a smaller font), which the port does not create; the `banner_text_layout` advance/y_off ladder is faithful but the smaller HFONT isn't built. | Every town/area name shipped so far is ≤ 22 chars; the town ("Town of Tonkiness", 17) uses font 6 exactly (the live LOGFONT). | an area name of length ≥ 23 is shown — create the font-8 HFONT (the retail `DAT_008a9274[8]` LOGFONT) + select it by length. |
| `ingame-letterbox` | `main.c`/`letterbox.c`: the establishing-shot cinematic letterbox bar heights are driven as a constant (`g_letterbox_top = g_letterbox_bottom = LETTERBOX_INTRO_BAR = 64`, armed in `enter_game`); retail writes them onto the scene-controller object's `+0x44`/`+0x48` from the unported `0x5a00c0` cutscene script (the same script source as the camera-pan trigger). The **grid-fill geometry itself is bit-exact** (`letterbox_render` = `0x48c150:124-162`, host-tested + blit-trace-verified) — only the *trigger/heights source* is synthetic. | 64/64 are the live-traced retail values across the whole establishing shot (blit trace flips 1450-1600); the town is the only in-game scene; the bars are scene-scoped (quirk #74). | the `0x5a00c0` cutscene script lands and writes the scene-object `+0x44`/`+0x48` (then the bars turn on/off + animate on the engine's schedule, incl. the establishing-shot→settled-gameplay transition). |
| `color-grade-gates` | `color_grade`/`main.c`: the colour-grade gates are hardcoded to the town defaults `gate1=700 gate2=850` (`COLOR_GRADE_TOWN_GATE1/2`); retail reads them from `*(*DAT_008a6e80 + 0x130)` / `+0x14c` (the launcher brightness/contrast config). | The defaults are the live-probed in-game values; the god-object config isn't modeled yet. | the launcher `config.dat` / `DAT_008a6e80` is modeled and the gates can differ from 700/850. |
| `dialogue-trigger` | `main.c`/`dialogue.h`: the town-intro dialogue bubble is armed on a MEASURED timer (`DIALOGUE_ARM_FRAMES`=**1282** flips after `game_enter` = arm on **sim tick 642**, first visible change t645 = retail's, TICK-AXIS calibrated ckpt 105 — the flip-axis +1298/+1300/+1304 readings were a constant 8 ticks late, the error absorbed by the pairing's drift; the whole pop/fade change SEQUENCE is pixel-identical at tick-equal after the shift) with line 1 hardcoded (`DIALOGUE_VA_TOWN_LINE1`/`_NAME_FATHER` exe-string VAs); the box geometry (174,148) + the tail anchor x (188 = the Father's screen center 378) are the line-1 constants of the `0x49c640` speaker-anchor math rather than computed from the live speaker. | The tick-642 arm / VAs / geometry are seed-pinned live values verified differ_px==0 at tick-equal through the pop window; precedent = the banner/camera-pan/letterbox triggers (same unported scene-script source). | the beat-runner (`0x439690` as a port-side beat sequence) + the town script `0x4d7d80` case `0x334be` land — the script then arms each line (speaker/face/voice/text VA) and `0x49c640` computes the box/tail from the live speaker anchor. |
| `cutscene-beat-runner` | `cutscene.c`: the town-intro chain sequencer (`0x4d7d80` → `0x439690`) drives the **arrival+house DIALOGUE lines** (Z-advance; the room chain, ckpt 123 — `dialogue-line-table` RETIRED) but NOT the interleaved non-dialogue beats: the opening/closing fades (beat type 2), the camera pans (type 3), the wait timers (type 6), and the actor emotes/run-offs (`0x401e60`/`0x402730`).  Those stay on the port's existing separately-validated MEASURED triggers (camera pan / banner / letterbox / scene-fade).  Also unused: the VOICED typewriter cadence (interval 6/0x12/0x18/0x24 when a voice id is set, `0x439690:508-514`) since audio is unported. | The dialogue lines (the user-visible "advance") are faithful + replay-verified (Father→Arche→Mother on Z); the non-dialogue beats already render correctly via the measured triggers; voice is a separate (audio) subsystem. | the sequencer drives the camera/letterbox/banner/timer/action beats too (retiring those measured triggers — `dialogue-trigger`/`ingame-camera-pan`/`ingame-letterbox`) so the arrival plays on the engine's schedule; voice lands with audio. |
| `cutscene-scene-chain` | `cutscene.c`: the town-intro chain now sequences arrival `0x334be` (10 lines) → house `0x334c8` (8 lines) and ENDS at the errands boundary `0x334dc` (ckpt 123, the harness-verified chain — quirk #103); the port hands control there.  Still unported: the errands ROOM itself — the separate flag-gated questline dispatcher `0x4dc510` (its own dialogue API `0x4a5ee0`, the storage-room/Sana sub-scenes) — and the LATER control-transfer LOCATION (post-errands → town flag-`0xd2` → Sana scene; the `entity+0x200=1` sites at `0x4d7d80:449-463`/719/882, which ckpt 122 found are NOT the first freeroam). | The arrival+house dialogue is the user-visible "dialogue advance"; the errands room is gameplay (a freeroam scene + questline) reached via `character_step` on live input (`controllable-arche-faithful.md`), a separate arc; the control MECHANISM (the bit-exact mover) is done. | the errands questline (`0x4dc510`) is ported + the freeroam hand-off runs `character_step` at the errands room (the first-freeroam control = `+0x200==0` char-AI), and the later `+0x200=1` transfer lands at its real post-errands location. |
| `cutscene-room-render` | `cutscene.c`/`main.c`: the chain sequences the house (`0x334c8`) + errands (`0x334dc`) dialogue, but their ROOM BACKDROPS are not loaded — the committed room key `room_state+0x4024` drives an unported map-load path (the `0x585ae0`/`0x586010` family + ~14 `+0x4024` consumers).  So the house lines currently play over the TOWN-ARRIVAL backdrop (retail's house is a multi-floor interior; the errands room a separate scene).  It is a LIGHT key swap (one game_enter; entities/room_state persist — quirk #103), so a render SELECTION, not a full reload. | The dialogue CONTENT + sequencing + room-key swap (CONTROL FLOW) are faithful, and the project splits logic-vs-render throughout (butterfly motion vs render; cutscene beats vs measured triggers); the backdrop is a tagged render gap, not a logic fake. | the room-key `+0x4024` drives the map-load/decode so the house + errands rooms render their real backdrops (the house lines play in the house; the errands room is the freeroam scene). |
| ~~`dialogue-portrait-per-speaker`~~ | **RETIRED (ckpt 123): the portrait RESOLVES per speaker.**  `portrait.c` ports the `0x49d6e0` face-table lookup (`DAT_006b6568`, embedded `portrait_face_data.c`): the speaker's head-state (`+0x1d8`, HARNESS-CAPTURED — Arche=100000101 / Father=100000211 / Mother=100000212, `runs/portrait-gt`) + the line's `face_id` → the portrait pool-slot, carried on the box (`portrait_slot`) and blitted by `game_render_dialogue` (slot, frame 0).  Replay-verified: the Father/Arche/Mother busts render per line (vs the old hardcoded slot 663 = a WRONG character, head 100000104). | — | — (retired; the remaining A/B head-FACING variant is `dialogue-portrait-facing`). |
| `dialogue-portrait-facing` | `cutscene.c`: each face-table record has 3 portrait-slot variants; the `+0x8` (A) vs `+0xa` (B) choice is the speaker's BODY-FACING (`0x49d6e0:143`, body+0x2c == 3 → A, else B), DYNAMIC per line (captured tally A=8/B=10 — the same speaker+face flips as the cast turns).  The port's cutscene cast is STATIC (no live per-line facing — `cutscene-party-chars`), so `CUTSCENE_PORTRAIT_VARIANT` = B (the default path + plurality); retail flips to A on the facing-3 lines.  `bVar4` (variant C, the old MVP's wrong pick) is ruled out (`in_ECX+0x2f0`==0 throughout — captured). | The right CHARACTER + expression render per line (the user's ask + the visible fix); A vs B is only the head-FACING art (left vs right), a subtle difference on some lines, genuinely unmodelable without the cast's live facing. | the animated party-band cast (`cutscene-party-chars`) gives each speaker a live body-facing → the resolver picks A vs B per line bit-exact. |
| `dialogue-arrow-art` | `main.c game_render_dialogue`: the advance arrow (shown while a finished line waits for Z) is NOT drawn — its bank lives on the widget manager (god+`0xb8c`) and the module is unresolved (the box_cell probe's res_id 1000 numerically collides with sotesd's parallax mountain sheet, quirk #92).  The CONFIG is RE'd + modelled (`0x410560`: frame base 0x14 + anim table {0,1,2,3}, one step per 10 updates, pos box+(368,92), 1px bob in the cel metrics; `dialogue_arrow_frame` tracks it). | The arrow is HIDDEN during typing (0x48d940's `+0x174[0]==1` early-out, confirmed on retail PNGs) and line 1 never finishes inside any current capture window — so the missing art cannot yet cause a pixel diff. | a module-aware re-probe pins the arrow bank's module+res (extend the box_cell probe to log the slot's `settings` HMODULE), register it, and draw on line-complete — needed before the first Z-advance capture. |
| `dialogue-pause-grades` | `dialogue.c dialogue_char_grade`: the typewriter pause grades are FITTED to the line-1 reveal trace (space 1 update, `,`/`.`/`!`/`?` 3×interval, row close +interval) — the builder writes grade slots interval/2i/3i/5i (`0x439690:499-505`) but the char→grade mapping lives in the unported reveal stepper.  **MEASURED diverging at the ROW TRANSITION (ckpt 105, tick axis):** across the row-1→row-2 boundary retail's change-tick gaps are {5, 14, 5} where the fitted model produces {1, 5, 16} (net −3 ticks; the port then types row 2 ~3-5 ticks early at tick-equal).  The steady 5-ticks/char cadence and the comma/space grades are confirmed exact. | The steady cadence + comma/space grades are tick-axis confirmed; the divergence is confined to the row-close boundary (one ~14-tick retail pause the fit mis-distributes); the USER's intro-1 @2463 mark is this residual (the ckpt-104 "zero-mean oscillation" read was a flip-axis pairing artifact — RETRACTED). | the reveal stepper fn is RE'd (which 0x414xxx steps `+0x170[4]/[6]`) and the exact char-class → grade-slot map (esp. the row-close grade) replaces the fit. |
| `dialogue-textwrap` | `dialogue.c dialogue_expand_text`: only the `%n` break + last-space word-wrap subset of the retail expander `0x43cf90` is ported (no escape sequences, ruby, >3-row paging). | Line 1 wraps identically under any last-space rule (host-tested vs the real exe string); English text carries no escapes. | a script line with an escape/ruby/paging construct renders (the full `0x43cf90` port, shared with `glyph-escape-expander`). |
| ~~`ingame-establishing-zoom`~~ | **RETIRED (ckpt 67): there is NO zoom.**  Live probe proved the in-game "establishing shot" is a leftward camera PAN at constant 1:1 scale (viewport `+0x64/+0x68` and shear `+0x74` constant; only `+0x60` animates; free-roam render path every frame; projector `0x490b90` has no scale).  The port's static `MAP_RENDER_CAM_TOWN_3F2` aligns with the retail hold golden at dx=0, same scale.  The remaining camera work (the pan itself) is `ingame-camera-snap`. | — | — (retired). |
| `title-loop-sidefx` | `title_scene` outer-loop side-effect hooks are stubbed: `0x5b1030` (message pump — replaced by the harness pacing), `0x43e140`/`0x40fe00` (audio/joystick update), `0x43c2e0` (per-entry child-widget anim). | None are the intro-pacing key (that was the driving cadence, fixed in `main_loop_body`); the title is bit-exact without them. | audio (milestone 3), joystick input, or in-row sub-widget animation lands. |
| `newgame-fadeout-render` | `newgame_drive`: the new-game→prologue fade-out re-renders the menu **opaque** for ~20 frames; retail fades the box-panel alpha (`0x48cf80` alpha arm via `0x5bd550`) + the GDI menu text. Transition *timing* is aligned (ckpt 48); the *render* is not bit-exact. | Self-contained polish; the timing (the load-bearing part) is correct. | the ~20 transition frames (port ~801-820) must be bit-exact vs retail's ~795-814 window. |
| `newgame-box-fadein` | `newgame_box`/`newgame_cursor`: the box fade-in (`0x48cf80` alpha arm) is not ported — the box pops in opaque. | Cosmetic; the settled box is bit-exact. | the fade-in frames are diffed. |
| `glyph-escape-expander` | `glyph_text`: the escape expander (`0x4034f0`/`0x4051d0`) is hooked NULL; the sprite-cell render mode (`0x48e200` `param_1==0`) and SJIS kinsoku word-wrap (`glyph_wrap` cls 3) are deferred. | English labels carry no escapes / SJIS; ASCII path is exact. | a string with an escape sequence, a sprite-cell, or Japanese text is rendered. |
| `assetreg-clone-defer` | `asset_register`: 94 `SS_MGR` clone calls + the 9 inline-clone clusters are deferred; some entries are observability-only placeholders pending extracted PE bytes. | The title/menu/new-game/prologue paths don't need the deferred sprite clones. | a scene that needs a deferred clone (the diff shows a missing sprite). |

## Verification gaps (not synthetic, but parity not yet *proven*)
| tag | gap | unblock |
|-----|-----|---------|
| `picker-flip-gate` | `newgame_picker` render can't be flip-diffed: retail's flip counter freezes inside the `0x565d10` modal pump, so flip-keyed capture/inject see only the title→menu transition. | a non-flip-keyed capture path (a Phase-B `scenario-test.py` anchor inside the modal pump). |
| ~~`fountain-rng-phase`~~ | **RESOLVED (ckpt 99).** The whole settled-town per-tick LCG stream is now bit-exact: the REGULAR consumers (butterflies `butterfly_step` + fountain/sky emitters, ckpt 98) plus the FOUR IRREGULAR ambient/event timers (`src/ambient.{c,h}`: `0x1136f`/`0x11370` `0x5531b0` sound, the wagon `0x1872d` idle-wander, the `0x467380` `0xe2a5` event timer — engine-quirk #95) are all ported in `0x46cd70` band order.  Validated: offline LCG replay 0/297 vs the seed-pinned capture, live port bit-exact ticks 0-248 (through all four fires 33/134/183/189), host test `ambient_pertick`.  The fountain/sky particle positions now read the same stream as retail → bit-exact (the `fountain-anchor`/`fountain-collide`/`particle-evict` rows below are separate, non-RNG residuals). | — | — (retired; residual `0x467380` cd-init = PORT-DEBT(ambient-event-cd)). |
| `ambient-event-cd` | `ambient.c`: the `0x467380` (`0xe2a5`) event timer's initial countdown is the seed-pinned constant `EVENT_CD_INIT=184` (so it fires at tick 183).  Retail sets `+0x20c` during the (unported) `0xe2a5` spawn arm `0x431cb0`, so the real value is RNG-derived from the spawn; the constant is exact only under `OSS_RNG_DEFAULT_SEED`.  Likewise the fire's conditional draws (`0x467380:30` + `0x4099a0`×2) are modelled as the always-4 count seen at tick 183 — a re-fire (tick ~588, past the validation window) whose gate conditions differ would draw fewer. | The seed is pinned on both sides (the parity methodology), so 184 → tick-183 fire is the ground-truth value; the only town fire in the 298-tick window is at 183 (4 draws). | the `0xe2a5` spawn arm `0x431cb0` is ported (so `+0x20c` derives from the spawn RNG) + the `0x467380` fire-gate conditions are RE'd (so re-fires past tick 298 draw the right count). |
| `fountain-anchor` | **NARROWED (ckpt 107) — the anchor VALUE is now empirically pinned + validated.** `main.c`: `FOUNTAIN_EMIT_{X,Y}_OFF` = **+1600 on both axes** (was X=+1245 / no Y).  The `0x557370` field-spec (`runs/r7-anchor-retail`) reads the fountain prop's `+0xc/+0x10` box = (6400,6400) and world = (176000,41600) — byte-identical to the port's prop world.  A stamp-equal-tick water-droplet blit match (port call-trace vs retail, tick 30, 27 droplets each, same cel/camera) drops the fountain-box differ **4286 → 560** at +1600.  **OPEN 2× formula gap:** the decompile (`0x557370:58`) reads `world + (+0xc)/2` = +3200, but the rendered spawn matches at +1600 = `+0xc/4`; root cause un-RE'd (a second halving in the `0x557550`/`0x426620` water-config, or a doubled `+0xc` unit). | +1600 is validated against the rendered spray (87% residual drop); the remaining ~560 is `fountain-collide` (below) + halo, NOT the anchor. | the 2× is resolved (the formula understood end-to-end) so the offset derives from the prop box, not an empirical constant. |
| `particle-evict` | `particle.c`: `0x557370`'s evict-oldest path (when all 1024 slots are full, evict the lowest-`+0x280`-age slot below the caller's priority) is not ported — a full pool just drops the spawn. | The fountain holds ~30 droplets in a 1024 slot pool, so it never fills; eviction never fires for this scene. | a scene's particle count approaches 1024 (the pool fills and a spawn must evict). |
| `fountain-collide` | `particle.c`: `0x46e510` case `0x18708` expires the droplet on a collision-grid water/solid hit (`(x,y)/0xc80 → mapctl+0x21c04`); the port approximates this with the `sub_phase==8` fade lifetime (~27 ticks).  **NOT observable in the town intro (ckpt 107):** per single flip the droplet count matches retail bit-exact (port 27 full / 6 lower-box == retail 27 / 6 at t30 — the fixed `sub_phase==8` ~27-tick lifetime happens to yield the same population as retail's collision expiry here).  An earlier "13 vs 6" reading was a 2-flips-per-tick doubling artifact.  Still a latent debt (no collision grid), just not a residual in this scene. | The lifetime cap reproduces retail's droplet population for the fountain. | a scene where the fixed lifetime ≠ the collision-hit lifetime (droplets visibly over/under-live). |
| ~~`fountain-fade`~~ | **RESOLVED (ckpt 107, USER-caught).**  The FRESH (sub_phase 0) droplets rendered too BRIGHT (blown-out pink-white) — the port drew all water via `g_ramp_a[10−sub_phase]` (group A, mode 1 ADD), but retail's config (`0x557550` case `0x18708` → `FUN_004385c0(DAT_008a9330)`) sets the fresh alpha to `ramp_b[10]` (group B, mode 0 NORMAL); the step only switches to ramp_a from sub_phase 1.  The 3 overlapping spawn-cluster droplets ADD-accumulated to white.  Fix: `particle_pool_render` renders sub_phase 0 via `ramp_b[10]`.  Upper-spray differ 154→0, box 560→305 (the rest is butterflies).  Host test `particle_render_emits` extended (fresh = ramp_b\|10, aged = ramp_a). | — | — (resolved; fountain spray bit-exact). |
| `scene-fade-window` (was `scene-fade-rng-phase`) | `main.c`/`scene_fade.c`: the establishing REVEAL iris VARIANT (the LCG draw `(rand*3)>>15`) is now **DRAWN at the correct post-spawn phase = 0/center-out** (ckpt 97, engine-quirk #94 — the full 213-draw room-load burst is replayed: 171 map via `effect_from_map` + **42 cutscene via `cutscene_cast`**, then the iris draw).  STILL synthetic: the port arms the reveal at `enter_game` with **no black-load window**, so the reveal's absolute START tick is offset from retail's by the skipped load frames (the engine holds black while the room streams in).  **TICK-AXIS signature (ckpt 105, intro-1):** at tick-equal the divergence = the whole ~20-row-per-side FADING FRONTIER band (the fading cells live 10 ticks × 4 rows started/tick), consistent with a ~1-tick lead — port reveal state k renders at tick k, retail's at ~k+1 — plus possible per-cell level deltas; the dt scan finds NO clean zero because the in-box particles ride on top.  The retail arm site (`0x439690:555-583`) runs in the SAME beat-runner invocation as the cinematic step `:1124`, so the extra tick lives in WHEN the script posts the arm request (the beat-runner driver), not in the update order. | The variant is now faithful (drawn, not pinned — host-tested + live-logged `variant=0`); the reveal MECHANISM + render + per-tick cadence are verified (blit trace, USER "looks reasonable"); the residual is the start-tick + a possible one-update offset, now precisely characterized. | the black-load window / beat-runner arm-request timing is modeled so the reveal starts at retail's tick (verify: tick-equal reveal box → differ_px==0 in a particle-free region). |
